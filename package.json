{
  "name": "roles-plugin-workspace",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "website"
  ],
  "scripts": {
    "build": "npm -ws run build",
    "dev": "npm -ws run dev"
  }
}
